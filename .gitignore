node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
export/photos.zip

# Local env + runtime uploads (photos are seeded from export/photos on deploy)
.env
.env.local
uploads

# Retrofit: raw dropped photos stay local; only the downscaled <id>.jpg +
# mapping.json are committed (they ship to the container for apply-originals).
export/originals_retrofit/incoming
